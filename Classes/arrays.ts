const array :string[] = []
const array2 : Array<string> = []  //the same as before

type User = {
    name: string
    isActive: boolean
}

const allUsers :User[] = []

const MLModel: number[][] = []  //array inside array of numbers


array.push('element')