enum SeatChoice{
    AISLE = 10,  //default is 0 and so on
    MIDDLE,  // 11 continues the enumeration
    WINDOW  // if using string the must assign string to each one
}

const hSeat = SeatChoice.AISLE